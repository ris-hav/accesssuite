// Used as-is for production builds (`ng build`, no configuration override).
// Placeholder until there's an actual deployed production API — replace
// once cloud deployment happens.
export const environment = {
  apiBase: 'https://api.accesssuite.example',
};
